package com.example.mallet;

public class LearningSetManager {
    /**private static LearningSet learningSet;

    // Method to fetch the LearningSet from the server (you need to implement this)
    public static void fetchLearningSet() {
        // Call your server API or perform network request to get the LearningSet
        // Update the static variable learningSet with the fetched data
        // Example:
        // learningSet = SomeApi.fetchLearningSetFromServer();
    }

    // Method to get the LearningSet (if already fetched)
    public static LearningSet getLearningSet() {
        return learningSet;
    }

    // Method to check if the LearningSet has been fetched
    public static boolean isLearningSetFetched() {
        return learningSet != null;
    }**/
}
